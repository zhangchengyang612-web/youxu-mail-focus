[package]
name = "wecom-mail-focus"
version = "0.6.7"
description = "Local-first WeCom mail classifier and reminder assistant"
authors = ["Mail Focus"]
edition = "2021"

[lib]
name = "wecom_mail_focus_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rusqlite = { version = "0.32", features = ["bundled"] }
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1", features = ["v4"] }
native-tls = "0.2"
imap = "2.4"
mailparse = "0.15"
html2text = "0.12"
thiserror = "2"
regex = "1"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "native-tls", "json"] }
lettre = { version = "0.11", default-features = false, features = ["builder", "smtp-transport", "native-tls"] }
base64 = "0.22"

[target.'cfg(target_os = "macos")'.dependencies]
security-framework = "3.7"

[target.'cfg(target_os = "windows")'.dependencies]
keyring = { version = "3", features = ["windows-native"] }
tokio = { version = "1", features = ["rt-multi-thread"] }
